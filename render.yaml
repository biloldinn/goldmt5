services:
  - type: web
    name: gold-signal-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python gold_signal_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
